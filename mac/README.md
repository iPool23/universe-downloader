# Mac Build Assets

This folder is used by the macOS build path.

Place the following files here before running the build on macOS:

- `mac/bin/ffmpeg`
- `mac/bin/ffprobe`
- `mac/icon.icns`

The Windows build continues to use `bin/`.
